<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hello <%= @user.first %> <%= @user.last %></h1>
    <h2>Thank you for booking with Archer Travel</h2>
    <% if @transaction_type == 'down payment' %>
  <h3>You paid a downpayment of <span class="green"><%= number_to_currency(@user.team.balance/2, :precision => 2) %></span></h3>
  <h3>You have a remaining balance of <span class="green"><%= number_to_currency(@user.team.balance, :precision => 2) %></span></h3>
  <h3>You must pay the remainder of your payment by XX/XX/XX</h3>
    <% elsif @transaction_type == 'paid balance' %>
    <h3>Thank you for paying your balance</h3>
    <% elsif @transaction_type == 'paid in full' %>
    <% end %>
    <h2>Rooms</h2>
<div class="table-responsive">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Hotel</th>
      <th>Address</th>
      <th>Room Number</th>
      <th>Smoking</th>
      <th>Occupancy</th>
      <th>Type</th>
      <th>Room Price</th>
    </tr>
  </thead>
  <tbody>
    <% @user.books.each do |val| %>
    <tr>
    <td><%= val.hotel.name %></td>
    <td><%= val.hotel.address %></td>
    <td><%= val.number %></td>
    <td><%= val.smoking %></td>
    <td><%= val.occupancy_a %></td>
    <td><%= val.room_type %></td>
    <td><%= number_to_currency(val.price, :precision => 2) %></td>
    </tr>
    <% end %>
    <tr>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
      <td></td>
      <td>Total:</td>
      <td class="green"><%= number_to_currency(@total, :precision => 2) %></td>
    </tr>
  </tbody>
</table>
</div>
  <h2>Guests</h2>
  <div class="table-responsive">
<table class="table table-striped">
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @user.first %></td>
      <td><%= @user.last %></td>
      <td>Adult</td>
    </tr>
    <% @user.guests.each do |val| %>
    <tr>
    <td><%= val.first %></td>
    <td><%= val.last %></td>
    <td><%= val.guest_type %></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>
  </body>
</html>